import { createSlice } from '@reduxjs/toolkit';

const initialState = {
    book: 0,
}

const boosSlice = createSlice({
    name: "books",
    initialState,
    reducers: {
        
    }

});


export default boosSlice.reducer;